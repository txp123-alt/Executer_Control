package com.txp.executer_control.util;

public class ADBExecuterUtil {

    public static String execute(String adbPath, String command)throws Exception{
        //Process exec = Runtime.getRuntime().exec(adbPath + " " + command);
        return null;
    }
}
